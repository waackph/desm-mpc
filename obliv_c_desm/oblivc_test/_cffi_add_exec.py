from _cffi_add import ffi, lib
#import pickle
import numpy as np
#import extract_word_embedds as ewe
##################################

party = 1
num = 1

lib.setup(party, num)

#for idx, score in enumerate(scores):
#	print(doc_title[idx], str(score), '\n')

##################################
